# Get a free key (no credit card required) at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_free_gemini_api_key_here

# Optional: override the model. gemini-2.5-flash is the default.
# If you hit free-tier rate limits, try gemini-flash-lite-latest instead.
GEMINI_MODEL=gemini-2.5-flash

PORT=3000
